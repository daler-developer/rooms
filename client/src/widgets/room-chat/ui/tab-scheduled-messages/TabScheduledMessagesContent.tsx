import BaseTabContent from "../base/BaseTabContent";
import ScheduledMessagesList from "../ScheduledMessages/ScheduledMessagesList";

const TabScheduledMessagesContent = () => {
  return (
    <BaseTabContent>
      <ScheduledMessagesList />
    </BaseTabContent>
  );
};

export default TabScheduledMessagesContent;
