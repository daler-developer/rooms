import { RegisterForm } from "@/modules/auth";

const Register = () => {
  return (
    <div className="flex justify-center pt-10">
      <RegisterForm />
    </div>
  );
};

export default Register;
